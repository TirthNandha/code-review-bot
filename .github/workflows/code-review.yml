# code-review.yml
# GitHub Actions workflow that triggers on pull_request events (opened, synchronize, reopened).
# Extracts the PR diff with `git diff`, then POSTs it to the deployed review service.
# The service URL and a GitHub token are injected via repository secrets.
